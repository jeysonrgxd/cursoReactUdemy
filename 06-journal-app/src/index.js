import React from 'react';
import ReactDOM from 'react-dom';
import { JournalApp } from './JournalApp';

import './styles/style.scss';

ReactDOM.render(
  // nombre de la aplicacion generica
  <JournalApp />,
  document.getElementById('root')
);